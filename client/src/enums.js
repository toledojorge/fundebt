const EDEBT_FILTER_SHOW = {
  ALL: "ALL",
  PAID: "PAID",
  UNPAID: "UNPAID",
};

const EDEBT_ORDER_BY = {
  AMOUNT: "amount",
  DATE: "createdAt",
  STATUS: "paid",
};

const EDEBT_ORDER_DIRECTION = {
  ASC: "asc",
  DESC: "desc",
};

export { EDEBT_FILTER_SHOW, EDEBT_ORDER_BY, EDEBT_ORDER_DIRECTION };
